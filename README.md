# Extract_-_Structure_Data_from_SDFs_pharmaceutical_documentation
# PDF Document Word Extraction with PyMuPDF

This project demonstrates how to extract all words along with their bounding box information from a PDF document using the `PyMuPDF` library.

## Setup

First, install the necessary library by running the following command in a code cell:

```python
!pip install pymupdf
